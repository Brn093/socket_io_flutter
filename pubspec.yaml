name: flutter_socket_io
description: Chat em Flutter & SocketIO

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  cupertino_icons: ^1.0.5
  dio: ^5.2.1+1
  socket_io_client: ^2.0.2
  flutter:
    sdk: flutter
  intl: ^0.18.1
  provider: ^6.0.5  
  mongo_dart: ^0.8.2

dev_dependencies:
  flutter_lints: ^2.0.2
  flutter_test:
    sdk: flutter

flutter:  
  uses-material-design: true
  
  assets:
    - assets/
  